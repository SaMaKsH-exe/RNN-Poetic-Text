<!-- @format -->

# RNN-Poetic-Text

## Streamlit app

Run the UI:

1. Install Streamlit (if needed):
   - `pip install streamlit`
2. Start the app:
   - `streamlit run src/main.py`

The app has two inputs:

- Temperature
- Length output
